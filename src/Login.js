import {Component} from 'react';

class Login extends Component {

    // const { email,setEmail,password,setPassword,handleLogin,handleSingup,hasAccount,setHasAccount,emailError,passwordError} = this.props;

    render(){
        return(
            <section className="login">

            </section>
        )
    }
}

export default Login;